namespace SleepingBearCinema;

class Program
{
    void Main(string[] args)
    {
        decimal consignBalance; // = apijsonfilekey;
        decimal rolloverBalance;

        decimal loopBalance = consignBalance + rolloverBalance;
        while (loopBalance >= 15.00m)
        {
            if (loopBalance >= 100.00m)
            {
                public void CreateGiftCard(100.0m);
                loopBalance = loopBalance - 100.00m;
            }

            if (loopBalance >= 50.00m)
            {
                public void CreateGiftCard(50.00m);
                loopBalance = loopBalance - 50.00m;
            }

            if (loopBalance >= 35.00m)
            {
                public void CreateGiftCard(35.00m);
                loopBalance = loopBalance - 35.00m;
            }

            if (loopBalance >= 25.00m)
            {
                public void CreateGiftCard(25.00m);
                loopBalance = loopBalance - 25.00m;
            }

            if (loopBalance >= 15.00m)
            {
                public void CreateGiftCard(15.00m);
                loopBalance = loopBalance - 15.00m;
            }

            decimal rooloverBalance = loopBalance;
        }
    }
}